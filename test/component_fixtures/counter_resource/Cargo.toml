[package]
name = "counter-resource"
version = "0.1.0"
edition = "2021"

[dependencies]
wit-bindgen = "0.41.0"

[build-dependencies]
wit-bindgen = "0.41.0"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true